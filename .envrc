use flake

# Paideia immersio Gmail API service account.
# Scoped here (not global shared-api.env) so it doesn't bind gemini-cli's
# OAuth login to the paideia-immersio GCP project in other folders.
export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.config/keys/gcp-paideia-sa.json"
