"""immersio-email subpackage — student-personalized PDF dispatch (spec 006).

Subcommand: ``immersio email --profile NAME --semester YYYY-N --course SLUG ...``.
Default mode is dry-run (no Gmail API call); ``--send`` enables the wire.
"""

__version__ = "0.1.0"
