"""Deterministic silver `진단×시험결합.parquet` writer (T020).

T002 scaffold stub. FR-014 / FR-015 / FR-030 — pyarrow ``use_dictionary=False``
+ ``write_statistics=False``; dict 컬럼 → ``json.dumps(ensure_ascii=False,
sort_keys=True)``; row sort by ``student_id`` ascending.
"""

__all__: list[str] = []
