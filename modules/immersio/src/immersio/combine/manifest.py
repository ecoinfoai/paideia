"""``manifest_phase3.json`` writer + schema_version verification (T017).

T002 scaffold stub. FR-021 / FR-024(exit 5) — SHA256 of 5 input silver files;
``packaging.version.Version`` SemVer compare (string lexicographic 비교 금지);
``json.dumps(indent=2, ensure_ascii=False, sort_keys=True)`` + trailing newline.
"""

__all__: list[str] = []
