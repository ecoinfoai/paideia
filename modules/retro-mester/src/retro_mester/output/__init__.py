"""retro_mester.output — Gold-layer output utilities (T017–T020).

Submodules:
- ``determinism`` — finalize_xlsx, dump_yaml, parquet_write_options.
- ``paths`` — bronze_dir, silver_dir, gold_dir, output_key.
- ``manager`` — archive_existing, atomic_write_bytes, atomic_write_text.
- ``manifest`` — build_manifest, write_manifest.
"""
